import Navbar from "../components/Navbar";

const New = () => {
    return (
        <section className="flex flex-col gap-8 md:flex-row w-full">
            <Navbar />
            <main>

            </main>
        </section>
    );
}

export default New;